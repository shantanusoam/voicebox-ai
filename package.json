{
  "name": "callbox-platform",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "CallBox application, audio gateway, console and hardware lab",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/dev.mjs",
    "check": "node --check web/app/app.js && node --check web/site/app.mjs",
    "test": "python -m pytest -q",
    "build": "python scripts/build.py"
  },
  "engines": {"node": ">=18"}
}
