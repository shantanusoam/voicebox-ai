[project]
name = "callbox-platform"
version = "0.2.0"
description = "Local-first voice gateway and front-desk workflow lab"
requires-python = ">=3.11"
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
